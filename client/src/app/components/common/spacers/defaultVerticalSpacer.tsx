import { DEFAULT_SPACING } from 'client/src/app/constants';

export function DefaultVerticalSpacer() {
  return <div style={{ height: '100%', width: DEFAULT_SPACING }} />;
}

export default DefaultVerticalSpacer;
