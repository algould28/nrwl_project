import colors from './colors';

export const theme = {
  colors: {
    text: colors.blackText,
    background: colors.surfaceGray,
    surface: '#FFFFFF',
    primary: colors.tealBlue,
    secondary: colors.cafeBrown,
    warning: colors.fireOpal,
    success: colors.forestGreen,
  },
};
